Usage:
git clone git://github.com/jduff/rails-templates.git
rails new APP_NAME -J -d mysql -m rails-templates/all.rb

After running the template cd into your new application and run 'bundle install'

Todo:
Split into separate files
